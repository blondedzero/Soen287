/* css file for login/register webpage */

/* Unversal Selector */
    * {
        margin: 0;
        box-sizing: border-box;
        font-family: 'Poppins', sans-serif;
    }

body {
    margin: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    background-image: url('entry-background.jpeg');
    background-size: cover;
    background-position: center;
}

/* Formatting the translucid containe around the login form */
    .container {
        width: 300px;
        height: 390px;
        position: relative;
        z-index: 1;
        background: rgba(255,255,255, 0.1);
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        border-radius: 10px;
        border: 1px solid #fff;
    }

    .container::before {
        content: '';
        position: absolute;
        width: 100%;
        height: 100%;
        border-radius: 10px;
        -webkit-backdrop-filter: blur(5px);
        backdrop-filter: blur(5px);
        z-index: -1;
    }

.login-box {
    max-width: 250px;
    margin: 0 auto;
    text-align: center;
}

h2 {
    color: #fff;
    margin-top:30px;
    margin-bottom: -20px;
}

form {
    display: flex;
    flex-direction: column;
    margin-top: 20px;
}

input {
    padding: 10px;
    margin-top: 25px;
    border: none;
    border-radius: 10px;
    background: transparent;
    border: 1px solid #fff;
    font-size: 13px;
}

input::placeholder {
    color: #fff;
}

input:focus {
    outline: none;
}

.options {
    display: flex;
    align-items: center;
    margin-top: 15px;
    font-size: 12px;
    color: #fff;
}

/* Formating the login options */
    .options input {
        margin-right: 5px;
        margin-top: 0px;
    }

    .options a {
        text-decoration: none;
        color: #fff;
        margin-left:auto;
    }

/* Formatting the login button */
    button {
        background: #cb9823;
        color:#fff;
        font-weight: bold;
        padding: 10px;
        border: none;
        border-radius: 10px;
        cursor: pointer;
        margin-top: 15px;
    }

    button:hover {
        background: transparent;
        color: #fff;
        outline: 1px solid #fff;
    }

/* Visibility Improvements */

    /* changing the colour of certain hyperlinks to improve visibility */
    #modified-link-color {
        color: #cb9823;
        text-shadow: 1px 1px 1px #000;
    }

    /* adding text shadow to anchors with the #modified-link-color */
    a#modified-link-color:visited {
        text-shadow: 1px 1px 1px #fff;
    }

p {
    font-size: 12px;
    color: #fff;
    margin-top: 15px;
}

img {
    display: block;
    margin: auto;
  }
